package com.ronnie.toastjet.model.enums

enum class BodyType {
    None,
    FormData,
    URLEncoded,
    Binary,
    RAW
}

enum class RawType{
    JSON,
    XML,
    TEXT,
    HTML,
    JS,
    GraphQL
}