export enum MessageType {
  Initialize,

  FilePicker,
  FileSaver,
  FileDelete,

  GetVariables,
  SaveVariables,
  LoadEnvironment,

  GetRawRequest,
  SaveRequest,
  GetResponse,
  GetResponseFromNonce,
}
